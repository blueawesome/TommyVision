# Logo

Put an optional main menu logo image here, for example:

```txt
assets/logo/tommyvision-logo.png
```

Enable or change the path with `ui.logo_path` in `config/library.yaml`. If the file is missing or invalid, TommyVision falls back to the normal text title.
