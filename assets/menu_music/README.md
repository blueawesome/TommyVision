# Menu Music

Put optional menu music files here.

Supported formats:

- `.mp3`
- `.ogg`
- `.wav`

Enable menu music with `audio.menu_music_enabled` in `config/library.yaml`. Music plays only in the menus, stops during video playback, and starts again after `mpv` exits.
