# TommyVision Assets

Optional local assets can live here during development or Pi deployment. The app works normally when these folders are empty or missing.

- `logo/`: optional main menu logo image
- `boot/`: optional startup video
- `menu_music/`: optional shuffled menu background music

Large media, audio, and image files are ignored by Git by default.
